# testSlot

API Automation Tests (JUnit 5 + Rest Assured)

Проект содержит набор автоматизированных API-тестов для сервиса testslotegrator.com, реализованных на Java 15, JUnit 5 и Rest Assured.

Тесты покрывают полный e2e-flow работы с пользователями:
от авторизации до создания, получения, удаления и проверки пустого списка.

🧱 Технологический стек

Java 15

Gradle

JUnit 5

Rest Assured

Jackson (JSON)

SLF4J + Logback (логирование ошибок)

src/test
 ├─ java
 │   ├─ config        # API config, specs, логирование, token storage
 │   ├─ tests         # e2e и CRUD API тесты
 │   └─ utils         # работа с JSON, генерация данных, сбор ID
 └─ resources
     ├─ data          # единый test-data.json
     └─ token         # сохранённый accessToken

📄 Test data

Все тестовые данные хранятся в одном файле test-data.json.

Поддерживается логика:

defaults — общие значения (например, email)

если значение поля пустое ("") → используется дефолт

если указано значение → оно применяется только для конкретного теста

Это позволяет легко управлять данными без дублирования JSON-ов.

🔐 Авторизация и токен

/tester/login — получение accessToken

токен сохраняется в файл

автоматически используется во всех последующих запросах через Authorization

🔄 e2e Flow

Тесты реализуют следующий сценарий:

/tester/login — получить токен

/automationTask/create — создать 12 пользователей в цикле

/automationTask/getOne — получить пользователя по email

/automationTask/getAll — получить список всех пользователей

/automationTask/deleteOne/{id} — удалить всех созданных пользователей

/automationTask/getAll — убедиться, что список пуст

Вся цепочка выполняется в одном e2e-тесте, что гарантирует порядок шагов и стабильность в CI.

🧪 Генерация тестовых данных

Для каждого пользователя динамически генерируются:

уникальный email

уникальный username

Это исключает конфликты и делает тесты нефлаковыми.

📋 Логирование

Логируются только ошибки

Используется SLF4J + Logback

Логи пишутся в консоль и файл

Request / Response автоматически логируются при:

HTTP ошибках (4xx / 5xx)

падении assertion

✅ Основные принципы

Чистые тесты без логики внутри

Один источник тестовых данных

Нет зависимости между отдельными тестами

Готово к запуску локально и в CI

Код легко расширяется под новые API
